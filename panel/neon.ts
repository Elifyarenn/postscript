// Neon project policy. The database schema is owned by Drizzle migrations
// (`drizzle/`), so nothing is declared here: no services, no functions.
import { defineConfig } from "@neon/config/v1";

export default defineConfig({});
